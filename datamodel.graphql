type User {
  id: ID! @id
  githubID: String! @unique
  following: [Follow!]
}

type Follow {
  id: ID! @id
  followed: User!
  githubID: String! @unique
  notes: [Notes!]
}

type Notes {
  id: ID! @id
  for: Follow!
  title: String!
  body: String!
}
