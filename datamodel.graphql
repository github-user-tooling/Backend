type User {
  id: ID! @id
  login: String! @unique
  accessToken: String! @unique
  favorites: [Favorite]
}

type Favorite {
  id: ID! @id
  login: String! @unique
  notes: [Notes]
}

type Notes {
  id: ID! @id
  title: String!
  body: String!
}
