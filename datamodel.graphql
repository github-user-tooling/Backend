type User {
  id: ID! @id
  githubID: String! @unique
  favorites: [Favorite]
}

type Favorite {
  id: ID! @id
  login: String! @unique
  notes: [Notes]
}

type Notes {
  id: ID! @id
  title: String!
  body: String!
}
